# Nginx Demo (docker-compose)

Run this demo locally using Docker Compose. It serves static HTML/CSS from a mounted volume into an Nginx container.

Quick start:

```bash
cd docker/docker-compose
docker compose up -d
```

Open http://localhost:8080 in your browser.

To stop and remove containers:

```bash
docker compose down
```
